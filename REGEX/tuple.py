# a tuple is like a list but is immutable,it uses a parenthesis instead of a square brackets during definition
measurements = (100,200)

print(measurements[0])

# while a list is mutable a tuple is immutable

measurements[0]=23
print(measurements[0])